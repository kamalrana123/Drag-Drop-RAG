# API base URL — proxied by Vite (dev) or Nginx (production)
# Default works out of the box; only change if your backend runs on a different port
VITE_API_URL=/api/v1

# Set to true to enable SSE token streaming in the chat panel
VITE_STREAMING=false
